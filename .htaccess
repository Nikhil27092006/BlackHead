# Prevent directory listing
Options -Indexes

# Deny access to sensitive files
<FilesMatch "\.(sql|log|bak|env|ini|sh|zip)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Prevent access to dotfiles
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
